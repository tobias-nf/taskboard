# Dev environment secrets — copy to secrets.dev.tfvars and fill in.
# Do not commit the filled file.

db_password = "replace-me-dev"

taskboard_api_secrets = {
  TASKBOARD_ADMIN_API_KEY = "hive_sk_hive-admin_replace-me-dev"
}
