# Copy this file to `infra/secrets.auto.tfvars` and fill in real values.
# Terraform will create/update the corresponding AWS Secrets Manager values.
#
# Do not commit the filled file.
# Important: these values will still be stored in Terraform state, so use an
# encrypted remote backend with tightly controlled access.

db_password  = "replace-me"

taskboard_api_secrets = {
  TASKBOARD_ADMIN_API_KEY = "hive_sk_hive-admin_replace-me"
}
